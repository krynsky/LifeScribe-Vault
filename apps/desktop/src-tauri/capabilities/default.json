{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability for the main window: core IPC and dialog pickers only. No shell, no frontend fs scope — all file access goes through purpose-built Rust commands.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
